<!DOCTYPE html>
<html>
<head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/css/style.css' />
</head>
<body>
{{#if expressFlash}}
    <p><strong>FLASH!</strong> {{ flashMessage }}</p>
    <p><strong>FLASH!</strong> {{ error }}</p>
    <p><strong>FLASH!</strong> {{ success }}</p>
    <p><strong>FLASH!</strong> {{ failureFlash }}</p>
    <p><strong>FLASH!</strong> {{ successFlash }}</p>
{{/if}}

{{{body}}}
</body>
</html>
